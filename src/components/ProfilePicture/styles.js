import { StyleSheet } from 'react-native';
const styles = StyleSheet.create({
  container: {
    margin: 7,
    borderRadius: 40,
    borderWidth: 2,
    borderColor: '#ae22e0',
  },
  image: {
    borderRadius: 40,
    borderWidth: 2,
    borderColor: '#ffffff',
  },
});

export default styles;
